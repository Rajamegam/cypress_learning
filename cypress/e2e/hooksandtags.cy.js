/**
 * notes: four types of hooks are available
 * 1. before --> executes before all the 'it' blocks
 * 2. beforeeach --> executes before each 'it' blocks
 * 3. after --> executes after all the 'it' blocks
 * 4. aftereach --> executes after each 'it' blocks
 */